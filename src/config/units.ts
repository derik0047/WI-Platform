/** Suggested line-item units (free text, offered via a datalist). */
export const COMMON_UNITS = [
  "pcs",
  "hour",
  "day",
  "month",
  "kg",
  "m",
  "m²",
  "liter",
  "service",
] as const;

export const DEFAULT_UNIT = "pcs";
