export const siteConfig = {
  name: "WI Platform",
  description: "The production foundation for RR Foundry Workflow-Intelligence applications.",
} as const;

export type SiteConfig = typeof siteConfig;
